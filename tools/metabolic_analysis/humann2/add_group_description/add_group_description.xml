<tool id="humann2_add_group_description" name="Add description feature">

    <description>on HUMAnN2 data</description>

    <macros>

    </macros>

    <requirements>
    </requirements>

    <stdio>
    </stdio>

    <version_command></version_command>

    <!--<command>-->
    <command interpreter="python">
        add_group_description.py 
            --input $input_file 
            --output $output_file 
            --group $group
    </command>

    <inputs>
        <param name="input_file" type="data" format="tsv" label="Gene family abundance table" help=""/>
        <param name="group" type="select" display="radio" label="Grouping systems" help="">
            <!--<option value="uniref50_ec">Enzyme commission (EC) categories</option>-->
            <!--<option value="uniref50_rxn">Metacyc reactions</option>-->
            <option value="uniref50_ko" selected="true">KEGG Orthogroups (KOs)</option>
            <!--<option value="uniref50_go">Subset of non-redundant, high-level Gene Ontology (GO) categories</option>-->
        </param>
    </inputs>

    <outputs>
        <data format="tsv" name="output_file" label="humann2_simplify_table_${input_file}" />
    </outputs>

    <tests>

    </tests>

    <help>
        **What it does**

        This script add description of the features in abundance table. 

        **Input**

        This script takes as arguments an abundance table which were previ.

        **Outputs**

        **Parameters**

    </help>

    <citations>
    </citations>
</tool>