<tool id="humann2_simplify_table" name="Simplify table features">

    <description>of HUMAnN2</description>

    <macros>

    </macros>

    <requirements>
    </requirements>

    <stdio>
    </stdio>

    <version_command></version_command>

    <!--<command>-->
    <command interpreter="python">
        simplify_table.py --input $input_file --output $output_file 
    </command>

    <inputs>
        <param name="input_file" type="data" format="tsv" label="Gene family abundance table" help=""/>
    </inputs>

    <outputs>
        <data format="tsv" name="output_file" label="humann2_simplify_table_${input_file}" />
    </outputs>

    <tests>

    </tests>

    <help>
        **What it does**

        This script remove the stratification in files generated by HUMAnN2 (gene families file, pathway coverage file
        or pathway abundance file). 

        **Input**

        This script takes as arguments an abundance table with a stratification at the community level.

        **Outputs**

        **Parameters**

    </help>

    <citations>
    </citations>
</tool>