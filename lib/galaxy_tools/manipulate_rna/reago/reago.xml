<tool id="reago" name="Assemble rRNA">

	<description>with Reago</description>

    <macros>
    </macros>

	<requirements>
    </requirements>

    <stdio>
    </stdio>

    <version_command>
    </version_command>

  	<!--<command>-->
    <command interpreter="python">
        reago_wrapper.py
            --src_dir $os.getcwd()
            --r1_input_sequence_file $r1_input_sequence_file
            --r2_input_sequence_file $r2_input_sequence_file
            --full_gene_sequence_file $full_gene_sequence_file
            --fragment_sequence_file $fragment_sequence_file
            --read_length $read_length
            --overlap $overlap
            --error_correction $error_correction
            --tip_size $tip_size
            --path_finding_parameter $path_finding_parameter
            --delete_tmp_dirpath True
    </command>

  	<inputs>
        <param name="r1_input_sequence_file" type="data" format="fasta" label="R1 input sequence file" help=""/>
        <param name="r2_input_sequence_file" type="data" format="fasta" label="R2 input sequence file" help=""/>

        <param name="read_length" type="integer" min="1" max="1000" value="1" label="Read length" help=""/>
        <param name="overlap" type="float" min="0" max="1" value="0.8" label="Overlap" help=""/>
        <param name="error_correction" type="float" min="0" max="1000" value="0.05" label="Error correction" help=""/>
        <param name="tip_size" type="integer" min="0" max="1000" value="30" label="Tip size" help=""/>
        <param name="path_finding_parameter" type="integer" min="2" max="11" value="10" label="Path finding parameter" help=""/>
  	</inputs>

  	<outputs>
        <data format="fasta" name="full_gene_sequence_file" label="full_gene_${r1_input_sequence_file.name}_${r2_input_sequence_file.name}"/>
        <data format="fasta" name="fragment_sequence_file" label="fragment_${r1_input_sequence_file.name}_${r2_input_sequence_file.name}"/>
  	</outputs>

  	<tests>
  
  	</tests>

  	<help>
        **What it does**

        Reago is a tool to reconstruct 16S rRNA genes in metagenomic data 

        **Input**

        

        **Outputs**

        


  	</help>

    <citations>
        <citation type="doi">10.1093/bioinformatics/btv231</citation>
    </citations>
</tool>

